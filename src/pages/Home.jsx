// import { useData } from '../hooks/useData';

const Home = () => {
  // const data = useData();

  return (
    <>
      <section>
        <h1>Home</h1>
      </section>
    </>
  );
};

export default Home;
